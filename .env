# BacPipes Environment Configuration

# ============================================
# Database Configuration
# ============================================
POSTGRES_USER=bacpipes
POSTGRES_DB=bacpipes
# Note: Uses service name 'postgres' with bridge networking (LXC compatible)
# External port 5434 maps to internal port 5432
DATABASE_URL="postgresql://bacpipes@postgres:5432/bacpipes"

# ============================================
# BACnet Network Configuration
# ============================================
# OPTIONAL: BACnet settings are now loaded from database SystemSettings
# These env vars serve as fallbacks only. Configure via Settings GUI instead.
# If not set, system will auto-detect local IP address.
# BACNET_IP=192.168.1.32
# BACNET_PORT=47808
# BACNET_DEVICE_ID=3001234

# ============================================
# MQTT Broker Configuration
# ============================================
# OPTIONAL: MQTT settings are loaded from database MqttConfig
# These env vars serve as fallbacks only. Configure via Settings GUI instead.
# MQTT_BROKER=10.0.60.3
# MQTT_PORT=1883
MQTT_CLIENT_ID=bacpipes_worker
# MQTT_USERNAME=
# MQTT_PASSWORD=

# ============================================
# System Configuration
# ============================================
TZ=UTC
NODE_ENV=production

# ============================================
# Application Settings
# ============================================
DEFAULT_POLL_INTERVAL=60
CONFIG_REFRESH_INTERVAL=60

# TimescaleDB (for CSV export)
# Note: Uses service name 'timescaledb' with bridge networking
# External port 5435 maps to internal port 5432
TIMESCALEDB_HOST=timescaledb
TIMESCALEDB_PORT=5432
TIMESCALEDB_DB=timescaledb
TIMESCALEDB_USER=bacpipes
TIMESCALEDB_PASSWORD=
