# Database connection (from parent .env)
# Note: Uses service name 'postgres' with bridge networking (LXC compatible)
# External port 5434 maps to internal port 5432
DATABASE_URL="postgresql://anatoli@postgres:5432/bacpipes"

# Session secret for authentication (32+ characters)
SESSION_SECRET="bacpipes-session-secret-change-in-production"
